---
  apiVersion: helm.fluxcd.io/v1
  kind: HelmRelease
  metadata:
    name: public-gateway
    namespace: istio-system
  spec:
    chart:
      repository: https://kube-champ.github.io/helm-charts
      name: istio-gateway
      version: "0.1.0"
    values:  # https://hub.helm.sh/charts/fluxcd/flux
      selector: istio-system/ingressgateway

      hosts:
        - "*.kubechamp.gq"