---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: promtail
  namespace: logging
spec:
  values:
    loki:
      serviceName: loki
      serviceScheme: http
    pipelineStages:
      - json:
          expressions:
            customer_id: "1"
      - tenant:
          source: "1"